package com.shangyi.${project}.dao.repository${module};

import com.shangyi.${project}.dao.entity${module}.${className}Entity;
import org.springframework.data.jpa.repository.JpaRepository;


public interface I${className}EntityRepository extends JpaRepository<${className}Entity, String> {

}
